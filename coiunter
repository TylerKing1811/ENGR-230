`timescale 1ns/1ps

module tb_counter;

reg clk;
reg reset;
wire [5:0] count;

counter_39 uut (
    .clk(clk),
    .reset(reset),
    .count(count)
);

// Clock generation
always #5 clk = ~clk;

initial begin
    clk = 0;
    reset = 1;

    #10 reset = 0;

    #500 $finish;
end

endmodule
